server:
  port: 8000

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_FIRST
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"
              - "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders:
              - Authorization
              - Cache-Control
              - Content-Type
              - Accept
            allowCredentials: true
            maxAge: 3600
      routes:
        - id: springboot-service
          uri: lb://pawster
          predicates:
            - Path=/api/auth/**, /api/animals/**, /api/adoptions/**, /api/users/**, /api/admin/**, /api/rehome/**, /api/missing-pets/**, /api/activity-logs/**, /uploads/**

        - id: php-admin-service
          uri: http://php:80
          predicates:
            - Path=/php/**
          filters:
            - RewritePath=/php(?<segment>/?.*), $\{segment}

        - id: php-service
          uri: http://php:80
          predicates:
            - Path=/api/products/**
          filters:
            - RewritePath=/api/products(?<segment>/?.*), $\{segment}

        - id: django-service
          uri: http://django:8001
          predicates:
            - Path=/api/approvals/**, /api/notifications/**, /api/surveys/**, /api/django/**, /media/**

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

jwt:
  secret: ${JWT_SECRET}